This is a small Flask project for COP4538 that acts as a basic contact manager. It stores contacts in memory, allows new contacts to be added, and supports searching for contacts by name using a simple linear search while handling cases where a contact is not found. The purpose of the project is to practice working with strings, basic searching, and simple web application logic in Python.
